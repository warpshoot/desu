diff --git a/pxlcam/index.html b/pxlcam/index.html
index 11d417b..59d33fe 100644
--- a/pxlcam/index.html
+++ b/pxlcam/index.html
@@ -4,7 +4,7 @@
 <head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
-  <title>PXL CAM</title>
+  <title>DESU邃｢ pxlcam</title>
   <style>
     @import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@300;400;500&display=swap');
 
@@ -59,12 +59,96 @@
       color: var(--accent);
     }
 
-    .logo span {
-      color: var(--text-dim);
-      font-weight: 300;
+    /* Credit Button */
+    #credit-btn {
+      position: fixed;
+      top: max(12px, env(safe-area-inset-top));
+      left: 12px;
+      width: 32px;
+      height: 32px;
+      border-radius: 50%;
+      border: 1px solid rgba(255, 255, 255, 0.3);
+      background: rgba(255, 255, 255, 0.1);
+      color: rgba(255, 255, 255, 0.5);
+      display: flex;
+      align-items: center;
+      justify-content: center;
+      font-size: 16px;
+      font-weight: bold;
+      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
+      cursor: pointer;
+      z-index: 100;
+      transition: all 0.2s;
+    }
+
+    #credit-btn:hover {
+      background: rgba(255, 255, 255, 0.2);
+      color: rgba(255, 255, 255, 0.8);
+      border-color: rgba(255, 255, 255, 0.5);
+    }
+
+    /* Credit Modal */
+    #credit-modal {
+      display: none;
+      position: fixed;
+      top: 0;
+      left: 0;
+      width: 100%;
+      height: 100%;
+      background: #fff;
+      z-index: 1001;
+      align-items: center;
+      justify-content: center;
+      padding: 20px;
+    }
+
+    #credit-modal.visible {
+      display: flex;
+    }
+
+    #credit-content {
+      background: #fff;
+      border: 1px solid rgba(0, 0, 0, 0.6);
+      padding: 20px 24px;
+      font-family: monospace;
+      width: 300px;
+      max-width: calc(100vw - 40px);
+    }
+
+    .credit-row {
+      display: flex;
+      gap: 12px;
+      line-height: 1.9;
+    }
+
+    .credit-key {
+      color: #888;
       font-size: 11px;
-      letter-spacing: 0.05em;
-      margin-left: 6px;
+      min-width: 4em;
+      flex-shrink: 0;
+    }
+
+    .credit-val {
+      font-size: 11px;
+      color: #111;
+      text-decoration: none;
+    }
+
+    a.credit-val {
+      text-decoration: underline;
+    }
+
+    .credit-divider {
+      border: none;
+      border-top: 1px solid #ddd;
+      margin: 12px 0;
+    }
+
+    .credit-manual-link {
+      font-size: 11px;
+      color: #111;
+      text-decoration: underline;
+      margin-right: 16px;
     }
 
     .status {
@@ -74,7 +158,7 @@
     }
 
     .status.live::before {
-      content: '';
+      content: "";
       display: inline-block;
       width: 5px;
       height: 5px;
@@ -212,11 +296,6 @@
       border-color: var(--accent);
     }
 
-    .styled-select:disabled {
-      opacity: 0.25;
-      cursor: not-allowed;
-    }
-
     .select-wrap {
       position: relative;
       flex: 1;
@@ -342,7 +421,7 @@
       background: transparent;
       border: 1px solid var(--accent);
       color: var(--accent);
-      font-family: 'DM Mono', monospace;
+      font-family: "DM Mono", monospace;
       font-size: 11px;
       letter-spacing: 0.12em;
       cursor: pointer;
@@ -458,148 +537,44 @@
     .spacer {
       flex: 1;
     }
-
-    .btn:disabled {
-      opacity: 0.25;
-      cursor: not-allowed;
-    }
-
-    /* Modal */
-    .modal-overlay {
-      position: fixed;
-      inset: 0;
-      background: rgba(0, 0, 0, 0.85);
-      z-index: 100;
-      display: flex;
-      align-items: flex-end;
-      justify-content: center;
-      padding: 16px;
-    }
-
-    .modal {
-      background: var(--surface);
-      border: 1px solid var(--border);
-      border-radius: 6px;
-      width: 100%;
-      max-width: 420px;
-      padding: 16px;
-      display: flex;
-      flex-direction: column;
-      gap: 14px;
-    }
-
-    .modal-header {
-      display: flex;
-      align-items: center;
-      justify-content: space-between;
-    }
-
-    .modal-title {
-      font-size: 11px;
-      letter-spacing: 0.15em;
-      color: var(--accent);
-    }
-
-    .modal-close {
-      background: none;
-      border: none;
-      color: var(--text-dim);
-      font-size: 18px;
-      cursor: pointer;
-      padding: 0 2px;
-      font-family: 'DM Mono', monospace;
-      line-height: 1;
-    }
-
-    .modal-close:hover {
-      color: var(--text);
-    }
-
-    .modal-preview-wrap {
-      aspect-ratio: 1;
-      background: var(--bg);
-      border: 1px solid var(--border);
-      border-radius: 3px;
-      overflow: hidden;
-      position: relative;
-    }
-
-    .modal-preview-wrap canvas {
-      width: 100%;
-      height: 100%;
-      image-rendering: pixelated;
-      image-rendering: crisp-edges;
-      display: block;
-    }
-
-    .modal-grid-overlay {
-      position: absolute;
-      inset: 0;
-      pointer-events: none;
-      opacity: 0.08;
-      background-image:
-        repeating-linear-gradient(to right, var(--text) 0, var(--text) 1px, transparent 1px, transparent),
-        repeating-linear-gradient(to bottom, var(--text) 0, var(--text) 1px, transparent 1px, transparent);
-    }
-
-    .modal-row {
-      display: flex;
-      align-items: center;
-      gap: 10px;
-    }
-
-    .modal-label {
-      font-size: 10px;
-      color: var(--text-dim);
-      letter-spacing: 0.1em;
-      text-transform: uppercase;
-      width: 40px;
-      flex-shrink: 0;
-    }
-
-    .seg-group {
-      display: flex;
-      gap: 4px;
-      flex: 1;
-    }
-
-    .seg-btn {
-      flex: 1;
-      padding: 7px 6px;
-      background: var(--bg);
-      border: 1px solid var(--border);
-      border-radius: 3px;
-      color: var(--text-dim);
-      font-family: 'DM Mono', monospace;
-      font-size: 10px;
-      letter-spacing: 0.06em;
-      cursor: pointer;
-      text-align: center;
-      transition: all 0.15s;
-    }
-
-    .seg-btn.active {
-      border-color: var(--accent);
-      color: var(--accent);
-    }
-
-    .modal-actions {
-      display: flex;
-      gap: 6px;
-    }
   </style>
 </head>
 
 <body>
 
+  <!-- Credit Button -->
+  <div id="credit-btn">?</div>
+
+  <!-- Credit Modal -->
+  <div id="credit-modal">
+    <div id="credit-content">
+      <div class="credit-row">
+        <span class="credit-key">蜷咲ｧｰ</span>
+        <span class="credit-val">DESU邃｢ pxlcam</span>
+      </div>
+      <div class="credit-row">
+        <span class="credit-key">謾ｯ邨ｦ蜈・/span>
+        <a href="https://warpshoot.github.io/desu/" target="_blank" class="credit-val">warpshoot.github.io/desu</a>
+      </div>
+      <div class="credit-row">
+        <span class="credit-key">陬ｽ騾</span>
+        <a href="https://warpshoot.github.io/" target="_blank" class="credit-val">wpy</a>
+      </div>
+      <hr class="credit-divider">
+      <div class="credit-row">
+        <span class="credit-key">隱ｬ譏取嶌</span>
+        <a href="MANUAL.md" target="_blank" class="credit-manual-link">莠ｺ髢灘髄縺・/a>
+      </div>
+    </div>
+  </div>
+
   <div class="app">
-    <div class="header">
-      <div class="logo">PXL CAM<span>DESU邃｢</span></div>
+    <div class="header" style="justify-content: flex-end">
       <div class="status" id="status">STANDBY</div>
     </div>
 
-    <div class="canvas-wrap">
-      <canvas id="pixelCanvas" width="16" height="16" style="touch-action:none;"></canvas>
+    <div class="canvas-wrap" id="canvasWrap">
+      <canvas id="pixelCanvas" width="16" height="16"></canvas>
       <div class="grid-overlay" id="gridOverlay"></div>
       <div class="corner-mark tl"></div>
       <div class="corner-mark tr"></div>
@@ -607,7 +582,6 @@
       <div class="corner-mark br"></div>
       <div class="start-screen" id="startScreen">
         <div class="icon">笳ｩ</div>
-        <p>CAMERA / IMAGE 竊・PIXEL ART</p>
         <div class="start-buttons">
           <button class="start-btn" id="startBtn">CAM</button>
           <button class="start-btn secondary" id="loadImgBtn">IMG</button>
@@ -619,6 +593,18 @@
     <canvas id="sampleCanvas" style="display:none;"></canvas>
     <input type="file" id="fileInput" accept="image/*" style="display:none;">
 
+    <div class="actions">
+      <button class="btn" id="freezeBtn">FREEZE</button>
+      <button class="btn" id="loadAnotherBtn">LOAD IMG</button>
+      <div class="select-wrap" style="flex:1;">
+        <select id="saveResolution" class="styled-select" style="width:100%; text-align:center; padding-right:20px;">
+          <option value="320">SAVE 320px</option>
+          <option value="640" selected>SAVE 640px</option>
+          <option value="1280">SAVE 1280px</option>
+        </select>
+      </div>
+    </div>
+
     <div class="controls">
       <!-- Palette -->
       <div class="control-row">
@@ -679,7 +665,7 @@
           <div class="dither-label">DITHER</div>
         </div>
         <div class="toggle-row">
-          <div class="toggle" id="gridToggle"></div>
+          <div class="toggle on" id="gridToggle"></div>
           <div class="dither-label">GRID</div>
         </div>
         <div class="spacer"></div>
@@ -687,78 +673,41 @@
       </div>
     </div>
 
-    <div class="actions">
-      <button class="btn" id="freezeBtn">FREEZE</button>
-      <button class="btn" id="loadAnotherBtn">LOAD IMG</button>
-      <button class="btn" id="saveOpenBtn" disabled>SAVE</button>
-    </div>
-  </div>
-
-  <div class="modal-overlay" id="saveModal" style="display:none;">
-    <div class="modal">
-      <div class="modal-header">
-        <span class="modal-title">EXPORT</span>
-        <button class="modal-close" id="modalClose">ﾃ・/button>
-      </div>
-      <div class="modal-preview-wrap">
-        <canvas id="modalPreview"></canvas>
-        <div class="modal-grid-overlay" id="modalGridOverlay" style="display:none;"></div>
-      </div>
-      <div class="modal-row">
-        <div class="modal-label">SIZE</div>
-        <div class="seg-group" id="sizeGroup">
-          <button class="seg-btn" data-size="320">320px</button>
-          <button class="seg-btn active" data-size="640">640px</button>
-          <button class="seg-btn" data-size="1280">1280px</button>
-        </div>
-      </div>
-      <div class="modal-row">
-        <div class="modal-label">GRID</div>
-        <div class="seg-group" id="modalGridGroup">
-          <button class="seg-btn" data-grid="on">ON</button>
-          <button class="seg-btn" data-grid="off">OFF</button>
-        </div>
-      </div>
-      <div class="modal-actions">
-        <button class="btn" id="modalCopyBtn">COPY</button>
-        <button class="btn primary" id="modalSaveBtn">SAVE</button>
-      </div>
-    </div>
   </div>
 
   <script>
     // === PALETTES ===
     const PALETTES = {
       full: {
-        name: 'FULL',
+        name: 'FULL COLOR',
         colors: null // no restriction
       },
       gb: {
-        name: 'GAMEBOY',
+        name: 'GREEN 4BIT',
         colors: ['#0f380f', '#306230', '#8bac0f', '#9bbc0f']
       },
       gbp: {
-        name: 'GB POCKET',
+        name: 'GRAY 4BIT',
         colors: ['#1a1a1a', '#545454', '#a8a8a8', '#e0e0e0']
       },
       cga: {
-        name: 'CGA',
+        name: 'CYAN MAGENTA',
         colors: ['#000000', '#55ffff', '#ff55ff', '#ffffff']
       },
       amber: {
-        name: 'AMBER',
+        name: 'AMBER PHOSPHOR',
         colors: ['#1a0800', '#6b3000', '#c76b00', '#ffb000']
       },
       vboy: {
-        name: 'V.BOY',
+        name: 'RED MATRIX',
         colors: ['#000000', '#550000', '#aa0000', '#ff0000']
       },
       pastel: {
-        name: 'PASTEL',
+        name: 'PASTEL DREAM',
         colors: ['#332941', '#a98fba', '#e8c8d4', '#fef4e0']
       },
       fc: {
-        name: 'FAMICOM',
+        name: '8BIT TILE',
         preview: ['#E40058', '#0078F8', '#F8B800', '#F8F8F8'],
         colors: [
           '#7C7C7C', '#0000FC', '#0000BC', '#4428BC', '#940084', '#A80020', '#A81000', '#881400', '#503000', '#007800', '#006800', '#005800', '#004058', '#000000',
@@ -768,13 +717,13 @@
         ]
       },
       sfc: {
-        name: 'SUPER FC',
+        name: '16BIT CHIP',
         preview: ['#ff0000', '#00ff00', '#0000ff', '#ffff00'],
         colors: null,
         quantize: 8 // Artificially drop it to 3-bit per channel for aesthetics (true SFC is 32)
       },
       mac: {
-        name: 'SYSTEM 7',
+        name: 'SYSTEM 216',
         preview: ['#ff00ff', '#00ffff', '#ffff00', '#cccccc'],
         colors: null,
         quantize: 6 // 216 web safe colors
@@ -795,24 +744,28 @@
     let saturation = 1;
     let hueShift = 0;
     let dither = false;
-    let showGrid = false;
+    let showGrid = true;
     let frozen = false;
     let stream = null;
     let facingMode = 'environment';
     let animId = null;
     let mode = null; // 'cam' or 'img'
     let loadedImage = null; // Image element for img mode
-    let customized = false;
 
-    let cropCenterX = 0.5;
-    let cropCenterY = 0.5;
-    let cropZoom = 1.0;
-    const CROP_ZOOM_MIN = 0.25;
-    const CROP_ZOOM_MAX = 8.0;
+    // Pan & Zoom state
+    let imgScale = 1;
+    let imgPanX = 0;
+    let imgPanY = 0;
+    let isDragging = false;
+    let startPanX = 0;
+    let startPanY = 0;
+    let lastMouseX = 0;
+    let lastMouseY = 0;
 
     const video = document.getElementById('videoFeed');
     const pixelCanvas = document.getElementById('pixelCanvas');
     const sampleCanvas = document.getElementById('sampleCanvas');
+    const canvasWrap = document.getElementById('canvasWrap');
     const pCtx = pixelCanvas.getContext('2d');
     const sCtx = sampleCanvas.getContext('2d');
 
@@ -833,13 +786,6 @@
       });
 
       palSelect.addEventListener('change', (e) => {
-        if (e.target.value === '__custom__') return;
-
-        // Remove CUSTOM option if present
-        const customOpt = palSelect.querySelector('option[value="__custom__"]');
-        if (customOpt) palSelect.removeChild(customOpt);
-        customized = false;
-
         currentPalette = e.target.value;
 
         // Reset all sliders back to baseline when a palette is chosen/re-chosen
@@ -868,50 +814,29 @@
     }
 
     buildSelectors();
-    document.getElementById('gridOverlay').style.display = 'none';
-
-    function updateSaveState() {
-      const canSave = (mode === 'img' && loadedImage) || (mode === 'cam' && frozen);
-      document.getElementById('saveOpenBtn').disabled = !canSave;
-    }
-
-    function setCustomMode() {
-      if (customized) return;
-      customized = true;
-      const palSelect = document.getElementById('paletteSelect');
-      const opt = document.createElement('option');
-      opt.value = '__custom__';
-      opt.textContent = 'CUSTOM';
-      palSelect.appendChild(opt);
-      palSelect.value = '__custom__';
-    }
 
     // === SLIDERS ===
     document.getElementById('brightness').addEventListener('input', e => {
       brightness = parseInt(e.target.value);
       document.getElementById('brightVal').textContent = brightness;
-      setCustomMode();
       if ((mode === 'img' && loadedImage) || frozen) renderFrame();
     });
 
     document.getElementById('contrast').addEventListener('input', e => {
       contrast = parseFloat(e.target.value);
       document.getElementById('contVal').textContent = contrast.toFixed(1);
-      setCustomMode();
       if ((mode === 'img' && loadedImage) || frozen) renderFrame();
     });
 
     document.getElementById('saturation').addEventListener('input', e => {
       saturation = parseFloat(e.target.value);
       document.getElementById('satVal').textContent = saturation.toFixed(1);
-      setCustomMode();
       if ((mode === 'img' && loadedImage) || frozen) renderFrame();
     });
 
     document.getElementById('hue').addEventListener('input', e => {
       hueShift = parseInt(e.target.value);
       document.getElementById('hueVal').textContent = hueShift;
-      setCustomMode();
       if ((mode === 'img' && loadedImage) || frozen) renderFrame();
     });
 
@@ -919,7 +844,6 @@
     document.getElementById('ditherToggle').addEventListener('click', function () {
       dither = !dither;
       this.classList.toggle('on', dither);
-      setCustomMode();
       if ((mode === 'img' && loadedImage) || frozen) renderFrame();
     });
 
@@ -927,92 +851,8 @@
       showGrid = !showGrid;
       this.classList.toggle('on', showGrid);
       document.getElementById('gridOverlay').style.display = showGrid ? '' : 'none';
-      if (document.getElementById('saveModal').style.display !== 'none') {
-        updateModalGridState();
-      }
     });
 
-    // === CROP GESTURES (img mode only) ===
-    (function () {
-      const canvas = pixelCanvas;
-      const ptrs = new Map();
-      let lastDragX = 0, lastDragY = 0;
-      let pinchStartDist = 0, pinchStartZoom = 1;
-      let lastTapTime = 0;
-
-      function clampCrop() {
-        if (!loadedImage) return;
-        const { width: iw, height: ih } = loadedImage;
-        const cropSide = Math.min(iw, ih) / cropZoom;
-        if (cropSide < iw) {
-          const h = cropSide / 2 / iw;
-          cropCenterX = Math.max(h, Math.min(1 - h, cropCenterX));
-        }
-        if (cropSide < ih) {
-          const h = cropSide / 2 / ih;
-          cropCenterY = Math.max(h, Math.min(1 - h, cropCenterY));
-        }
-      }
-
-      canvas.addEventListener('pointerdown', e => {
-        if (mode !== 'img' || !loadedImage) return;
-        e.preventDefault();
-        ptrs.set(e.pointerId, { x: e.clientX, y: e.clientY });
-        canvas.setPointerCapture(e.pointerId);
-
-        if (ptrs.size === 1) {
-          lastDragX = e.clientX;
-          lastDragY = e.clientY;
-          const now = Date.now();
-          if (now - lastTapTime < 300) {
-            cropCenterX = 0.5; cropCenterY = 0.5; cropZoom = 1.0;
-            renderFrame();
-          }
-          lastTapTime = now;
-        } else if (ptrs.size === 2) {
-          const [a, b] = [...ptrs.values()];
-          pinchStartDist = Math.hypot(b.x - a.x, b.y - a.y);
-          pinchStartZoom = cropZoom;
-        }
-      }, { passive: false });
-
-      canvas.addEventListener('pointermove', e => {
-        if (mode !== 'img' || !loadedImage || !ptrs.has(e.pointerId)) return;
-        e.preventDefault();
-        ptrs.set(e.pointerId, { x: e.clientX, y: e.clientY });
-
-        if (ptrs.size === 1) {
-          const { width: iw, height: ih } = loadedImage;
-          const cropSide = Math.min(iw, ih) / cropZoom;
-          const rect = canvas.getBoundingClientRect();
-          cropCenterX -= (e.clientX - lastDragX) / rect.width  * cropSide / iw;
-          cropCenterY -= (e.clientY - lastDragY) / rect.height * cropSide / ih;
-          clampCrop();
-          lastDragX = e.clientX;
-          lastDragY = e.clientY;
-          renderFrame();
-        } else if (ptrs.size === 2) {
-          const [a, b] = [...ptrs.values()];
-          const dist = Math.hypot(b.x - a.x, b.y - a.y);
-          cropZoom = Math.max(CROP_ZOOM_MIN, Math.min(CROP_ZOOM_MAX, pinchStartZoom * dist / pinchStartDist));
-          clampCrop();
-          renderFrame();
-        }
-      }, { passive: false });
-
-      const endPtr = e => { ptrs.delete(e.pointerId); };
-      canvas.addEventListener('pointerup', endPtr);
-      canvas.addEventListener('pointercancel', endPtr);
-
-      canvas.addEventListener('wheel', e => {
-        if (mode !== 'img' || !loadedImage) return;
-        e.preventDefault();
-        cropZoom = Math.max(CROP_ZOOM_MIN, Math.min(CROP_ZOOM_MAX, cropZoom * (e.deltaY > 0 ? 0.9 : 1.1)));
-        clampCrop();
-        renderFrame();
-      }, { passive: false });
-    })();
-
     // === IMAGE MODE ===
     const fileInput = document.getElementById('fileInput');
 
@@ -1027,7 +867,12 @@
       img.onload = () => {
         loadedImage = img;
         mode = 'img';
-        cropCenterX = 0.5; cropCenterY = 0.5; cropZoom = 1.0;
+
+        // Reset pan & zoom
+        imgScale = 1;
+        imgPanX = 0;
+        imgPanY = 0;
+
         // Stop camera if running
         if (stream) {
           stream.getTracks().forEach(t => t.stop());
@@ -1039,7 +884,7 @@
         document.getElementById('status').textContent = 'IMAGE';
         document.getElementById('status').className = 'status';
         document.getElementById('freezeBtn').textContent = 'CAM';
-        updateSaveState();
+        canvasWrap.style.cursor = 'grab';
         renderFrame();
       };
       img.src = URL.createObjectURL(file);
@@ -1056,19 +901,13 @@
 
       if (mode === 'img' && loadedImage) {
         const iw = loadedImage.width, ih = loadedImage.height;
-        const cropSide = Math.min(iw, ih) / cropZoom;
-        const cx = cropCenterX * iw, cy = cropCenterY * ih;
-        const srcL = cx - cropSide / 2, srcT = cy - cropSide / 2;
-        const clampL = Math.max(0, srcL), clampT = Math.max(0, srcT);
-        const clampR = Math.min(iw, srcL + cropSide), clampB = Math.min(ih, srcT + cropSide);
-        const scale = res / cropSide;
-        sCtx.fillStyle = '#000';
-        sCtx.fillRect(0, 0, res, res);
-        sCtx.drawImage(loadedImage,
-          clampL, clampT, clampR - clampL, clampB - clampT,
-          (clampL - srcL) * scale, (clampT - srcT) * scale,
-          (clampR - clampL) * scale, (clampB - clampT) * scale
-        );
+        const side = Math.min(iw, ih) / imgScale;
+
+        // Default center
+        let sx = (iw - side) / 2 - imgPanX;
+        let sy = (ih - side) / 2 - imgPanY;
+
+        sCtx.drawImage(loadedImage, sx, sy, side, side, 0, 0, res, res);
       } else if (mode === 'cam') {
         if (frozen) {
           sCtx.drawImage(freezeCanvas, 0, 0, freezeCanvas.width, freezeCanvas.height, 0, 0, res, res);
@@ -1235,7 +1074,6 @@
         document.getElementById('status').className = 'status live';
         frozen = false;
         document.getElementById('freezeBtn').textContent = 'FREEZE';
-        updateSaveState();
         loop();
       } catch (err) {
         console.error(err);
@@ -1282,7 +1120,6 @@
         document.getElementById('status').textContent = 'LIVE';
         document.getElementById('status').className = 'status live';
       }
-      updateSaveState();
     };
 
     // === COLOR QUANTIZATION ===
@@ -1329,116 +1166,156 @@
       animId = requestAnimationFrame(loop);
     }
 
-    // === SAVE MODAL ===
-    let saveSize = 640;
+    // === SAVE ===
+    document.getElementById('saveResolution').addEventListener('change', (e) => {
+      const selectEl = e.target;
+      const targetSize = parseInt(selectEl.value);
 
-    function buildExportCanvas(targetSize) {
       const res = currentRes;
-      const c = document.createElement('canvas');
-      c.width = targetSize;
-      c.height = targetSize;
-      const ctx = c.getContext('2d');
-      ctx.imageSmoothingEnabled = false;
-      ctx.drawImage(pixelCanvas, 0, 0, targetSize, targetSize);
-      if (showGrid) {
-        const cell = targetSize / res;
-        ctx.strokeStyle = 'rgba(204, 204, 204, 0.08)';
-        ctx.lineWidth = 1;
-        ctx.beginPath();
-        for (let i = 1; i < res; i++) {
-          const p = i * cell;
-          ctx.moveTo(p, 0); ctx.lineTo(p, targetSize);
-          ctx.moveTo(0, p); ctx.lineTo(targetSize, p);
-        }
-        ctx.stroke();
+      const exportCanvas = document.createElement('canvas');
+      exportCanvas.width = targetSize;
+      exportCanvas.height = targetSize;
+      const eCtx = exportCanvas.getContext('2d');
+      eCtx.imageSmoothingEnabled = false;
+      eCtx.drawImage(pixelCanvas, 0, 0, targetSize, targetSize);
+
+      const link = document.createElement('a');
+      link.download = `pxlcam_${res}x${res}_to_${targetSize}px_${Date.now()}.png`;
+      link.href = exportCanvas.toDataURL('image/png');
+      link.click();
+
+      // UI Feedback
+      const originalText = selectEl.options[1].text; // Store "SAVE 640px"
+      selectEl.options[selectEl.selectedIndex].text = 'SAVED...';
+      selectEl.style.color = 'var(--accent)';
+
+      setTimeout(() => {
+        selectEl.selectedIndex = 1; // reset back to default view after action
+        selectEl.options[1].text = originalText;
+        selectEl.options[0].text = 'SAVE 320px'; // just in case
+        selectEl.options[2].text = 'SAVE 1280px';
+        selectEl.style.color = '';
+      }, 800);
+    });
+
+    // === PAN & ZOOM (IMG MODE ONLY) ===
+    canvasWrap.addEventListener('wheel', (e) => {
+      if (mode !== 'img' || !loadedImage) return;
+      e.preventDefault();
+
+      const zoomFactor = -e.deltaY * 0.002;
+      let newScale = imgScale * (1 + zoomFactor);
+
+      // limit scale
+      newScale = Math.max(0.5, Math.min(10, newScale));
+      imgScale = newScale;
+
+      renderFrame();
+    }, { passive: false });
+
+    // Mouse events
+    canvasWrap.addEventListener('mousedown', (e) => {
+      if (mode !== 'img' || !loadedImage) return;
+      isDragging = true;
+      startPanX = imgPanX;
+      startPanY = imgPanY;
+      lastMouseX = e.clientX;
+      lastMouseY = e.clientY;
+      canvasWrap.style.cursor = 'grabbing';
+    });
+
+    window.addEventListener('mousemove', (e) => {
+      if (!isDragging) return;
+      const dx = e.clientX - lastMouseX;
+      const dy = e.clientY - lastMouseY;
+
+      // map screen pixels to image source pixels based on ratio
+      const side = Math.min(loadedImage.width, loadedImage.height);
+      const ratio = side / canvasWrap.clientWidth / imgScale;
+
+      imgPanX = startPanX + (dx * ratio);
+      imgPanY = startPanY + (dy * ratio);
+
+      renderFrame();
+    });
+
+    window.addEventListener('mouseup', () => {
+      if (isDragging) {
+        isDragging = false;
+        canvasWrap.style.cursor = (mode === 'img') ? 'grab' : 'crosshair';
       }
-      return c;
-    }
+    });
 
-    function updateModalGridState() {
-      document.querySelectorAll('#modalGridGroup .seg-btn').forEach(b => {
-        b.classList.toggle('active',
-          (b.dataset.grid === 'on' && showGrid) || (b.dataset.grid === 'off' && !showGrid)
+    // Touch events
+    let initialPinchDist = 0;
+
+    canvasWrap.addEventListener('touchstart', (e) => {
+      if (mode !== 'img' || !loadedImage) return;
+      if (e.touches.length === 1) {
+        isDragging = true;
+        startPanX = imgPanX;
+        startPanY = imgPanY;
+        lastMouseX = e.touches[0].clientX;
+        lastMouseY = e.touches[0].clientY;
+      } else if (e.touches.length === 2) {
+        isDragging = false;
+        initialPinchDist = Math.hypot(
+          e.touches[0].clientX - e.touches[1].clientX,
+          e.touches[0].clientY - e.touches[1].clientY
         );
-      });
-      const res = currentRes;
-      document.getElementById('modalGridOverlay').style.display = showGrid ? '' : 'none';
-      document.getElementById('modalGridOverlay').style.backgroundSize =
-        `calc(100% / ${res}) calc(100% / ${res})`;
-    }
+      }
+    }, { passive: false });
 
-    function openSaveModal() {
-      const preview = document.getElementById('modalPreview');
-      preview.width = currentRes;
-      preview.height = currentRes;
-      preview.getContext('2d').drawImage(pixelCanvas, 0, 0);
-      updateModalGridState();
-      document.getElementById('saveModal').style.display = 'flex';
-    }
+    window.addEventListener('touchmove', (e) => {
+      if (mode !== 'img' || !loadedImage) return;
 
-    document.getElementById('saveOpenBtn').onclick = openSaveModal;
+      if (e.touches.length === 1 && isDragging) {
+        e.preventDefault();
+        const dx = e.touches[0].clientX - lastMouseX;
+        const dy = e.touches[0].clientY - lastMouseY;
 
-    document.getElementById('modalClose').onclick = () => {
-      document.getElementById('saveModal').style.display = 'none';
-    };
+        const side = Math.min(loadedImage.width, loadedImage.height);
+        const ratio = side / canvasWrap.clientWidth / imgScale;
 
-    document.getElementById('sizeGroup').addEventListener('click', e => {
-      const btn = e.target.closest('.seg-btn');
-      if (!btn) return;
-      saveSize = parseInt(btn.dataset.size);
-      document.querySelectorAll('#sizeGroup .seg-btn').forEach(b => b.classList.remove('active'));
-      btn.classList.add('active');
-    });
+        imgPanX = startPanX + (dx * ratio);
+        imgPanY = startPanY + (dy * ratio);
 
-    document.getElementById('modalGridGroup').addEventListener('click', e => {
-      const btn = e.target.closest('.seg-btn');
-      if (!btn) return;
-      showGrid = btn.dataset.grid === 'on';
-      document.getElementById('gridToggle').classList.toggle('on', showGrid);
-      document.getElementById('gridOverlay').style.display = showGrid ? '' : 'none';
-      updateModalGridState();
+        renderFrame();
+      } else if (e.touches.length === 2) {
+        e.preventDefault();
+        const currentDist = Math.hypot(
+          e.touches[0].clientX - e.touches[1].clientX,
+          e.touches[0].clientY - e.touches[1].clientY
+        );
+
+        const diff = currentDist - initialPinchDist;
+        const zoomFactor = diff * 0.01;
+        let newScale = imgScale * (1 + zoomFactor);
+        newScale = Math.max(0.5, Math.min(10, newScale));
+
+        imgScale = newScale;
+        initialPinchDist = currentDist; // reset base for continuous smooth zoom
+        renderFrame();
+      }
+    }, { passive: false });
+
+    window.addEventListener('touchend', () => {
+      isDragging = false;
     });
 
-    document.getElementById('modalSaveBtn').onclick = () => {
-      const res = currentRes;
-      const dataURL = buildExportCanvas(saveSize).toDataURL('image/png');
-      const a = document.createElement('a');
-      a.download = `pxlcam_${res}x${res}_${saveSize}px_${Date.now()}.png`;
-      a.href = dataURL;
-      document.body.appendChild(a);
-      a.click();
-      document.body.removeChild(a);
-      const btn = document.getElementById('modalSaveBtn');
-      const orig = btn.textContent;
-      btn.textContent = 'SAVED';
-      setTimeout(() => { btn.textContent = orig; }, 1200);
-    };
+    // === CREDIT MODAL ===
+    const creditBtn = document.getElementById('credit-btn');
+    const creditModal = document.getElementById('credit-modal');
 
-    function openImageTab(canvas) {
-      const url = canvas.toDataURL('image/png');
-      const win = window.open();
-      win.document.write(`<body style="margin:0;background:#000"><img src="${url}" style="max-width:100%;height:auto;image-rendering:pixelated;display:block"></body>`);
-      win.document.close();
-    }
-
-    document.getElementById('modalCopyBtn').onclick = () => {
-      const c = buildExportCanvas(saveSize);
-      if (navigator.clipboard && typeof ClipboardItem !== 'undefined') {
-        c.toBlob(async blob => {
-          try {
-            await navigator.clipboard.write([new ClipboardItem({ 'image/png': blob })]);
-            const btn = document.getElementById('modalCopyBtn');
-            const orig = btn.textContent;
-            btn.textContent = 'COPIED';
-            setTimeout(() => { btn.textContent = orig; }, 1200);
-          } catch {
-            openImageTab(c);
-          }
-        }, 'image/png');
-      } else {
-        openImageTab(c);
+    creditBtn.addEventListener('click', () => {
+      creditModal.classList.add('visible');
+    });
+
+    creditModal.addEventListener('click', (e) => {
+      if (e.target === creditModal) {
+        creditModal.classList.remove('visible');
       }
-    };
+    });
   </script>
 </body>
 
